name: Barium-CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: install deps
      run: |
        pip install conan
        sudo apt-get install cmake
    - name: build the target
      run: |
        make
